apiVersion: v1
kind: Service
metadata:
  name: {{ include "drinksaver-web.fullname" . }}
  labels:
    {{- include "drinksaver-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drinksaver-web.selectorLabels" . | nindent 4 }}
